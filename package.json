{
  "name": "quant-lab-operator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1",
    "check": "node scripts/deploy-worker.mjs --dry-run",
    "deploy": "node scripts/deploy-worker.mjs"
  },
  "devDependencies": {
    "wrangler": "4.71.0"
  }
}
