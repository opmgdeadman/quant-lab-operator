{
  "name": "quant-lab-operator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:python": "python -m pytest",
    "validate": "npm test && python -m pytest",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.71.0"
  }
}
